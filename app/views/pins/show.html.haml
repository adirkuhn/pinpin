%p.notice
  = notice

%h1= @pin.title
%p= @pin.description

%p= link_to 'Edit', edit_pin_path
%p= link_to 'Delete', @pin, method: :delete, data: { :confirm => "Are you sure?" }
%p= link_to 'Back', pins_path
